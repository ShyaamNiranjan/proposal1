/**
 * Design system — Xoom Store (premium home & lifestyle)
 *
 * Palette (Atelier Forest):
 * - canvas:  #F3F1EC  warm stone page background
 * - paper:   #FFFEFB  elevated surfaces / cards
 * - ink:     #1C1917  primary text
 * - mist:    #7A746A  secondary text
 * - line:    #E4DFD6  borders / dividers
 * - forest:  #1F4D3A  primary accent (confident, unexpected for home goods)
 * - moss:    #2F6B52  accent hover
 * - brass:   #9C7E4F  secondary highlight (pricing, badges)
 * - alert:   #B45309  low-stock / warnings
 *
 * Typography:
 * - Display: Fraunces (soft serif with optical character)
 * - Body:    Outfit (clean geometric sans)
 *
 * Signature element:
 * Dual-image product card — primary image crossfades to secondary on hover,
 * with a brass corner tick. One memorable moment; everything else stays quiet.
 *
 * Tokens are applied via @theme in src/index.css (Tailwind v4).
 */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
}
